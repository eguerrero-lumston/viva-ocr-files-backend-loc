var io =  require('ic-sockets').IO()
var main = io.of('/');
main.on('connection',function(socket){
    //Example
    //Ejemplo
    /**
     * new SocketController(socket)
     * or
     * socket.on("event",(data)=>{
     *     
     * })
     */
});